<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.2.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop-3.2.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.2.xsd "
	default-lazy-init="true">
	<description>Quartz计划任务调度配置</description>
	<context:property-placeholder location="classpath:spring/job.properties"
		ignore-unresolvable="true" />
	<!-- 数据分析定时下发工作类 -->
	<bean id="quartzJob" class="com.sxfusion.time.Test" />
	<!-- 定义调用对象和调用对象的方法 -->
	<bean id="jobTask"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<!-- 调用的类 -->
		<property name="targetObject">
			<ref bean="quartzJob" />
		</property>
		<!-- 调用类中的方法 -->
		<property name="targetMethod">
			<value>work</value>
		</property>
		<property name="concurrent" value="false" />
	</bean>
	<!-- 定义触发时间 -->
	<bean id="jobTaskTrigger"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail">
			<ref bean="jobTask" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<value>*/5 * * * * ?</value>
		</property>
	</bean>
	<!-- 定义触发时间 -->
	<bean id="tow"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail">
			<ref bean="towTask" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<value>*/5 * * * * ?</value>
		</property>

	</bean>

	<!-- 解决bean无法注入的问题 -->
	<bean id="jobFactory" class="com.sxfusion.demo.util.JobFactory"></bean>
	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean"
		id="schedulerFactoryBean" lazy-init="true" autowire="no">
		<property name="triggers">
			<list>
				<!--注册定时任务 -->
				<!-- <ref bean="jobTaskTrigger" /> -->
			</list>
		</property>
		<property name="jobFactory" ref="jobFactory" />
	</bean>


	<!-- 总管理类 如果将lazy-init='false'那么容器启动就会执行调度程序 -->
	<bean id="taskManager" lazy-init="false" autowire="no"
		class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<!-- 启动方法 -->
				<!-- <ref bean="jobTaskTrigger"/> <ref bean="tow"/> -->
			</list>
		</property>
		<!-- 在web关闭的时候关闭线程 -->
		<property name="waitForJobsToCompleteOnShutdown" value="true" />
	</bean>




	<bean id="startQuartz" lazy-init="true" autowire="no"
		class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<!-- <ref bean="cronTrigger" /> -->
			</list>
		</property>
	</bean>

	<bean id="quartzManager" class="com.util.time.QuartzManager"
		lazy-init="false" init-method="startJobs">
		<!--这个对象一定要注入，这样类才能进行管理，还有在类型要用get set方法，不然会报错。 -->
		<property name="scheduler" ref="startQuartz" />
	</bean>
</beans>